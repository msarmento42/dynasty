name: Auto-Merge Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    name: Auto-merge disabled for private repo
    runs-on: ubuntu-latest
    steps:
      - run: echo "Auto-merge is disabled for dynasty until CI and scope checks are explicitly trusted."
